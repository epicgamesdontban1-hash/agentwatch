{
  "name": "agentwatch-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Aggregates live session state streamed from AgentWatch mod clients and exposes it over REST/WebSocket for a website.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "ws": "^8.18.0"
  }
}
